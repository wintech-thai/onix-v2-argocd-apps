apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: onix-admin-ing
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.org/client-max-body-size: 50m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.onixAdminDomain }}
  rules:
    - host: {{ .Values.onixAdminDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: onix-v2-{{ .Values.envGroup }}-onix-admin
                port:
                  number: 80
