apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: smtp-secret
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: gcp-secret-store      # name of the SecretStore (or kind specified)
  target:
    name: smtp-secret      # name of the k8s Secret to be created
    creationPolicy: Owner
  data:
  - secretKey: SMTP_USER
    remoteRef:
      key: "google-smtp-secret"
      property: SMTP_USER
      version: latest

  - secretKey: SMTP_PASSWORD
    remoteRef:
      key: "google-smtp-secret"
      property: SMTP_PASSWORD
      version: latest
